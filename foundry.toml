[profile.default]
src = 'src'
out = 'out'
libs = ['lib']
extra_output_files = ['abi']

[rpc_endpoints]
arbitrum = "${ARBITRUM_RPC_MAINNET}"
arbitrum-goerli = "${ARBITRUM_RPC_TESTNET}"

# See more config options https://github.com/foundry-rs/foundry/tree/master/config
